import React from 'react';
import { HeaderContainer, HeaderContent, HeaderTitle } from './styles';

export const Header = () => {
  return (
    <HeaderContainer>
      <HeaderContent>
        <HeaderTitle>Desempenho das Lojas</HeaderTitle>
      </HeaderContent>
    </HeaderContainer>
  );
};
